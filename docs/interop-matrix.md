# Interoperability Matrix

This document records tested `.lrc` sidecar behavior by server/client version.

Initial MVP goal:

- verify Navidrome ingestion of `.lrc`
- verify OpenSubsonic lyrics retrieval for timed and untimed `.lrc`

